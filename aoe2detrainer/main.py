[
    {
        "path": "README.md",
        "content": "# Age of Empires II: Definitive Edition Trainer\n\nA Python-based trainer for Age of Empires II: Definitive Edition that provides various in-game modifications and enhancements.\n\n## Features\n\n- Resource modification (gold, wood, food, stone)\n- Population cap bypass (up to 500)\n- Instant building/unit creation\n- Map reveal\n- Speed hack\n\n## Installation\n\n1. Clone the repository:\n```bash\ngit clone https://github.com/yourusername/age-of-empires-2-trainer.git\ncd age-of-empires-2-trainer\n```\n\n2. Install dependencies:\n```bash\npip install -r requirements.txt\n```\n\n## Usage\n\nRun the trainer:\n```bash\npython main.py\n```\n\nUse the following hotkeys:\n- `F1` - Add 10,000 Gold\n- `F2` - Add 10,000 Wood\n- `F3` - Add 10,000 Food\n- `F4` - Add 10,000 Stone\n- `F5` - Max Population\n- `F6` - Instant Build\n- `F7` - Reveal Map\n- `F8` - Speed Hack (2x)\n\n## Disclaimer\n\nThis project is for educational purposes only. Use at your own risk in single-player games only.\n\n## License\n\nMIT"
    },
    {
        "path": "requirements.txt",
        "content": "pymem>=1.8.0\nkeyboard>=0.13.5\npsutil>=5.9.0"
    },
    {
        "path": "main.py",
        "content": "\"\"\"\nAge of Empires II: Definitive Edition Trainer - Main Entry Point\n\"\"\"\nimport sys\nimport time\nfrom src.memory_manager import MemoryManager\nfrom src.trainer import Trainer\nfrom src.hotkey_listener import HotkeyListener\n\ndef main():\n    \"\"\"Initialize and run the trainer.\"\"\"\n    try:\n        memory_manager = MemoryManager()\n        trainer = Trainer(memory_manager)\n        listener = HotkeyListener(trainer)\n        \n        print(\"Age of Empires II: Definitive Edition Trainer\")\n        print(\"Waiting for game process...\")\n        \n        while True:\n            if memory_manager.attach_to_process():\n                print(\"Game found! Hotkeys active.\")\n                listener.start()\n                break\n            time.sleep(1)\n        \n        try:\n            while True:\n                time.sleep(0.1)\n        except KeyboardInterrupt:\n            print(\"\\nExiting...\")\n            listener.stop()\n            sys.exit(0)\n            \n    except Exception as e:\n        print(f\"Error: {e}\")\n        sys.exit(1)\n\nif __name__ == \"__main__\":\n    main()"
    },
    {
        "path": "src/__init__.py",
        "content": "\"\"\"\nAge of Empires II: DE Trainer - Source Package\n\"\"\"\nfrom .memory_manager import MemoryManager\nfrom .trainer import Trainer\nfrom .hotkey_listener import HotkeyListener\n\n__all__ = ['MemoryManager', 'Trainer', 'HotkeyListener']"
    },
    {
        "path": "src/memory_manager.py",
        "content": "\"\"\"\nMemory manager for interacting with Age of Empires II: DE process.\nHandles process attachment, memory reading/writing, and pointer resolution.\n\"\"\"\nimport pymem\nimport pymem.process\nimport psutil\n\nclass MemoryManager:\n    \"\"\"Manages memory operations for the AoE2:DE process.\"\"\"\n    \n    def __init__(self):\n        self.process_name = \"AoE2DE_s.exe\"\n        self.pm = None\n        self.base_address = None\n        self.attached = False\n        \n    def attach_to_process(self) -> bool:\n        \"\"\"\n        Attach to the AoE2:DE process.\n        \n        Returns:\n            bool: True if successfully attached, False otherwise.\n        \"\"\"\n        try:\n            self.pm = pymem.Pymem(self.process_name)\n            self.base_address = pymem.process.module_from_name(\n                self.pm.process_handle, \n                self.process_name\n            ).lpBaseOfDll\n            self.attached = True\n            return True\n        except pymem.exception.ProcessNotFound:\n            self.attached = False\n            return False\n        except Exception as e:\n            print(f\"Failed to attach: {e}\")\n            self.attached = False\n            return False\n    \n    def read_int(self, address: int) -> int:\n        \"\"\"Read an integer from the specified memory address.\"\"\"\n        if not self.attached:\n            raise RuntimeError(\"Not attached to process\")\n        return self.pm.read_int(address)\n    \n    def write_int(self, address: int, value: int) -> None:\n        \"\"\"Write an integer to the specified memory address.\"\"\"\n        if not self.attached:\n            raise RuntimeError(\"Not attached to process\")\n        self.pm.write_int(address, value)\n    \n    def read_float(self, address: int) -> float:\n        \"\"\"Read a float from the specified memory address.\"\"\"\n        if not self.attached:\n            raise RuntimeError(\"Not attached to process\")\n        return self.pm.read_float(address)\n    \n    def write_float(self, address: int, value: float) -> None:\n        \"\"\"Write a float to the specified memory address.\"\"\"\n        if not self.attached:\n            raise RuntimeError(\"Not attached to process\")\n        self.pm.write_float(address, value)\n    \n    def read_bytes(self, address: int, size: int) -> bytes:\n        \"\"\"Read bytes from the specified memory address.\"\"\"\n        if not self.attached:\n            raise RuntimeError(\"Not attached to process\")\n        return self.pm.read_bytes(address, size)\n    \n    def write_bytes(self, address: int, data: bytes) -> None:\n        \"\"\"Write bytes to the specified memory address.\"\"\"\n        if not self.attached:\n            raise RuntimeError(\"Not attached to process\")\n        self.pm.write_bytes(address, data)\n    \n    def resolve_pointer(self, base_offset: int, offsets: list) -> int:\n        \"\"\"\n        Resolve a multi-level pointer chain.\n        \n        Args:\n            base_offset: Base address offset from module base\n            offsets: List of offsets for pointer chain\n            \n        Returns:\n            int: Final resolved address\n        \"\"\"\n        if not self.pm:\n            raise RuntimeError(\"Not attached to process\")\n        \n        address = self.base_address + base_offset\n        for offset in offsets[:-1]:\n            address = self.pm.read_int(address) + offset\n        return address + offsets[-1]"
    },
    {
        "path": "src/trainer.py",
        "content": "\"\"\"\nCore trainer functionality for Age of Empires II: DE.\nProvides methods to modify game resources, population, and other values.\n\"\"\"\nfrom typing import Optional\nfrom .memory_manager import MemoryManager\n\nclass Trainer:\n    \"\"\"Main trainer class with game modification methods.\"\"\"\n    \n    # Memory offsets (example offsets - would need to be updated for each game version)\n    RESOURCE_BASE = 0x04A1E2C0\n    RESOURCE_OFFSETS = [0x10, 0x28, 0x40, 0x58]  # gold, wood, food, stone\n    POPULATION_OFFSET = 0x04A1E3A0\n    SPEED_ADDRESS = 0x04A1E4B0\n    MAP_REVEAL = 0x04A1E500\n    INSTANT_BUILD = 0x04A1E560\n    \n    def __init__(self, memory_manager: MemoryManager):\n        \"\"\"\n        Initialize the trainer with a memory manager instance.\n        \n        Args:\n            memory_manager: MemoryManager instance for process interaction\n        \"\"\"\n        self.memory = memory_manager\n        self.resource_multiplier = 10000\n        self.speed_multiplier = 2.0\n        \n    def add_gold(self, amount: Optional[int] = None) -> bool:\n        \"\"\"Add gold to the player's resources.\"\"\"\n        try:\n            addr = self.memory.resolve_pointer(self.RESOURCE_BASE, [self.RESOURCE_OFFSETS[0]])\n            current = self.memory.read_int(addr)\n            self.memory.write_int(addr, current + (amount or self.resource_multiplier))\n            return True\n        except Exception as e:\n            print(f\"Failed to add gold: {e}\")\n            return False\n    \n    def add_wood(self, amount: Optional[int] = None) -> bool:\n        \"\"\"Add wood to the player's resources.\"\"\"\n        try:\n            addr = self.memory.resolve_pointer(self.RESOURCE_BASE, [self.RESOURCE_OFFSETS[1]])\n            current = self.memory.read_int(addr)\n            self.memory.write_int(addr, current + (amount or self.resource_multiplier))\n            return True\n        except Exception as e:\n            print(f\"Failed to add wood: {e}\")\n            return False\n    \n    def add_food(self, amount: Optional[int] = None) -> bool:\n        \"\"\"Add food to the player's resources.\"\"\"\n        try:\n            addr = self.memory.resolve_pointer(self.RESOURCE_BASE, [self.RESOURCE_OFFSETS[2]])\n            current = self.memory.read_int(addr)\n            self.memory.write_int(addr, current + (amount or self.resource_multiplier))\n            return True\n        except Exception as e:\n            print(f\"Failed to add food: {e}\")\n            return False\n    \n    def add_stone(self, amount: Optional[int] = None) -> bool:\n        \"\"\"Add stone to the player's resources.\"\"\"\n        try:\n            addr = self.memory.resolve_pointer(self.RESOURCE_BASE, [self.RESOURCE_OFFSETS[3]])\n            current = self.memory.read_int(addr)\n            self.memory.write_int(addr, current + (amount or self.resource_multiplier))\n            return True\n        except Exception as e:\n            print(f\"Failed to add stone: {e}\")\n            return False\n    \n    def set_population(self, max_pop: int = 500) -> bool:\n        \"\"\"Set the maximum population cap.\"\"\"\n        try:\n            addr = self.memory.resolve_pointer(self.POPULATION_OFFSET, [0x0])\n            self.memory.write_int(addr, max_pop)\n            return True\n        except Exception as e:\n            print(f\"Failed to set population: {e}\")\n            return False\n    \n    def enable_instant_build(self, enable: bool = True) -> bool:\n        \"\"\"Enable or disable instant building/unit creation.\"\"\"\n        try:\n            addr = self.memory.resolve_pointer(self.INSTANT_BUILD, [0x0])\n            self.memory.write_int(addr, 1 if enable else 0)\n            return True\n        except Exception as e:\n            print(f\"Failed to toggle instant build: {e}\")\n            return False\n    \n    def reveal_map(self, reveal: bool = True) -> bool:\n        \"\"\"Reveal or hide the map.\"\"\"\n        try:\n            addr = self.memory.resolve_pointer(self.MAP_REVEAL, [0x0])\n            self.memory.write_int(addr, 1 if reveal else 0)\n            return True\n        except Exception as e:\n            print(f\"Failed to toggle map reveal: {e}\")\n            return False\n    \n    def set_speed(self, multiplier: float = 2.0) -> bool:\n        \"\"\"Set game speed multiplier.\"\"\"\n        try:\n            addr = self.memory.resolve_pointer(self.SPEED_ADDRESS, [0x0])\n            self.memory.write_float(addr, multiplier)\n            return True\n        except Exception as e:\n            print(f\"Failed to set speed: {e}\")\n            return False"
    },
    {
        "path": "src/hotkey_listener.py",
        "content": "\"\"\"\nHotkey listener for the trainer.\nListens for key presses and triggers trainer functions.\n\"\"\"\nimport threading\nimport keyboard\nfrom .trainer import Trainer\n\nclass HotkeyListener:\n    \"\"\"Manages hotkey registration and callback execution.\"\"\"\n    \n    def __init__(self, trainer: Trainer):\n        \"\"\"\n        Initialize with a trainer instance.\n        \n        Args:\n            trainer: Trainer instance to call methods on\n        \"\"\"\n        self.trainer = trainer\n        self.running = False\n        self._thread = None\n        self._setup_hotkeys()\n        \n    def _setup_hotkeys(self) -> None:\n        \"\"\"Register all hotkey callbacks.\"\"\"\n        keyboard.add_hotkey('F1', self.trainer.add_gold, args=())\n        keyboard.add_hotkey('F2', self.trainer.add_wood, args=())\n        keyboard.add_hotkey('F3', self.trainer.add_food, args=())\n        keyboard.add_hotkey('F4', self.trainer.add_stone, args=())\n        keyboard.add_hotkey('F5', self.trainer.set_population, args=(500,))\n        keyboard.add_hotkey('F6', self.trainer.enable_instant_build, args=(True,))\n        keyboard.add_hotkey('F7', self.trainer.reveal_map, args=(True,))\n        keyboard.add_hotkey('F8', self.trainer.set_speed, args=(2.0,))\n        \n    def start(self) -> None:\n        \"\"\"Start the hotkey listener in a background thread.\"\"\"\n        if self.running:\n            return\n        self.running = True\n        self._thread = threading.Thread(target=self._listen, daemon=True)\n        self._thread.start()\n        print(\"Hotkey listener started. Press F1-F8 to activate features.\")\n        \n    def _listen(self) -> None:\n        \"\"\"Main listener loop.\"\"\"\n        while self.running:\n            keyboard.wait()\n            \n    def stop(self) -> None:\n        \"\"\"Stop the hotkey listener.\"\"\"\n        self.running = False\n        keyboard.unhook_all()\n        print(\"Hotkey listener stopped.\")\n        \n    def __del__(self):\n        self.stop()"
    },
    {
        "path": "tests/__init__.py",
        "content": "\"\"\"\nTests package for Age of Empires II: DE Trainer\n\"\"\""
    },
    {
        "path": "tests/test_trainer.py",
        "content": "\"\"\"\nUnit tests for the Trainer class.\nUses mocking to simulate memory operations without needing the game.\n\"\"\"\nimport unittest\nfrom unittest.mock import MagicMock, patch\nfrom src.trainer import Trainer\nfrom src.memory_manager import MemoryManager\n\nclass TestTrainer(unittest.TestCase):\n    \"\"\"Test suite for Trainer class.\"\"\"\n    \n    def setUp(self):\n        \"\"\"Set up test fixtures.\"\"\"\n        self.mock_memory = MagicMock(spec=MemoryManager)\n        self.trainer = Trainer(self.mock_memory)\n        \n    def test_add_gold_success(self):\n        \"\"\"Test adding gold succeeds.\"\"\"\n        self.mock_memory.resolve_pointer.return_value = 0x1234\n        self.mock_memory.read_int.return_value = 100\n        \n        result = self.trainer.add_gold(5000)\n        \n        self.assertTrue(result)\n        self.mock_memory.write_int.assert_called_once_with(0x1234, 5100)\n        \n    def test_add_gold_failure(self):\n        \"\"\"Test adding gold handles errors.\"\"\"\n        self.mock_memory.resolve_pointer.side_effect = Exception(\"Memory error\")\n        \n        result = self.trainer.add_gold(5000)\n        \n        self.assertFalse(result)\n        \n    def test_add_wood_success(self):\n        \"\"\"Test adding wood succeeds.\"\"\"\n        self.mock_memory.resolve_pointer.return_value = 0x5678\n        self.mock_memory.read_int.return_value = 200\n        \n        result = self.trainer.add_wood(3000)\n        \n        self.assertTrue(result)\n        self.mock_memory.write_int.assert_called_once_with(0x5678, 3200)\n        \n    def test_set_pop
